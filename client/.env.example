# URL base da API. Em desenvolvimento deixe em branco ou "/api": o Vite faz
# proxy de /api para o backend em localhost:4000 (ver vite.config.ts).
# Em produção, se o frontend e a API estiverem no mesmo domínio (via nginx),
# "/api" também funciona.
VITE_API_URL=/api
